<?xml version="1.0" encoding="UTF-8" ?>
<solr sharedLib="lib" persistent="true">
  <cores adminPath="/admin/cores" defaultCoreName="<%= @cores.first %>">
    <% @cores.each do |core| %>
    <core name="<%= core %>" instanceDir="cores/<%= core %>/"/>
    <% end %>
  </cores>
</solr>
